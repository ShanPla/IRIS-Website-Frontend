import "./Settings.css";

export default function Settings() {
  return (
    <div className="settings">
      <h1 className="settings-title">Settings</h1>
      <p className="settings-empty">Configuration options will appear here.</p>
    </div>
  );
}